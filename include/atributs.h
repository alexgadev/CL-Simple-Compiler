#include <stdlib.h>
#include <stdbool.h>
#include <string.h> //for usage of strdup
#include <math.h>

typedef struct{
    int integer;
    float floating;
    char* string;
    bool boolean;

    int type;
} atributs;
